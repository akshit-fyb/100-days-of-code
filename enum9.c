#include <stdio.h>

enum Levels { LOW = 5, MEDIUM = 10, HIGH = 15 };

int main()
{
    printf("LOW = %d\n", LOW);
    printf("MEDIUM = %d\n", MEDIUM);
    printf("HIGH = %d\n", HIGH);

    return 0;
}
